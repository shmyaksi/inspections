if (!a) { m = b } else { m = c }
let m = !a ? b : c


let user;
    function abcd(){
        let bc = 'mother';
        if (!bc) {
            let m=b;
            console.log('access denied');
        }
        else m=c;
    }
