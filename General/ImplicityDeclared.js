ddd = 4; // todo 'insert var declaration'

function i() {
    ade = 3;
    console.log(ad)
}

i()


class person23 {
    greet = ''; //todo?
    constructor(name) {
        this.name = name
    }

    present() {
        let greett = 'hello,';
        return greett + this.name
    }
}

//anya = new person23('anya')
//console.log(anya.present())

let globalVariable = 7;

function aGlobal(param) {
    var scopedToFunction = {
        nested: 3
    };

//    anotherGlobal = {};

}
aGlobal(3)

const map88 = new Map([['foo', 'bar'], ['baz', 42]])
function hh() {
    for (let k = 0; k < 2; k++) {
        objg = Object.fromEntries(map88);
        console.log(objg);
        break
    }
}
hh()




let namename = "charles"

 greet1 = namename => `Hello, ${namename}` // todo 'Insert var declaration for 'greet1' into previous var statement' produces red code

 capitalize1 = str => str.toUpperCase()

 exclaim1 = str => `${str}!!!`  //todo 'convert to name function' behaves strange?


for (let i=0;i<3;i++) {
    let loudGreeting = namename
        /*|> greet1
        |> capitalize1
        |> exclaim1*/
    break
}

/*function test1(param = throw new Error('required!')) {
    test2 = param === true ?? throw new Error('Falsy!');

}

test1(2)*/

/*function fas(param) {
    let i;
    ii ??= "default";
    while (ii === 'default') {
        console.log('ok')
        break
    }
}

fas(4)*/




