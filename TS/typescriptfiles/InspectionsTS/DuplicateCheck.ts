function padLeft(value: string, padding: string | number | string) {

    return

}

padLeft('wall', 45);

function extend(p: string): string & string & any {

    return
}

extend('hello');

function one(x: number): number & number {

    return x * x
}

console.log(one(2))
